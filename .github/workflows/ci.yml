name: OCaml CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-latest]
        ocaml-compiler: [5.0.0]

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Use OCaml ${{ matrix.ocaml-compiler }}
      uses: ocaml/setup-ocaml@v3
      with:
        ocaml-compiler: ${{ matrix.ocaml-compiler }}

    - name: Compile Native Binary
      run: |
        opam exec -- ocamlopt -w +a-4-40-42-70 -o microgpt microgpt.ml
        opam exec -- ocamlopt -w +a-4-40-42-70 -o fastgpt fastgpt.ml

    - name: Run Smoke Test
      run: opam exec -- ./microgpt | head -n 20

    - name: Verify Bit-Level Parity
      run: |
        opam exec -- ./microgpt | head -n 12 > micro_out.txt
        opam exec -- ./fastgpt | head -n 12 > fast_out.txt
        diff micro_out.txt fast_out.txt

  reference-parity:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Use OCaml 5.0.0
      uses: ocaml/setup-ocaml@v3
      with:
        ocaml-compiler: 5.0.0

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Verify Mathematical Parity with Python Reference
      run: opam exec -- python3 .github/scripts/verify_parity.py
